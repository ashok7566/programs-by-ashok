using namespace std;
#include<iostream>
int main()
{

    int a;
    cout<<"Enter an integer:";
    cin>>a;
    if(a>0)
        cout<<"it is a positive no."<<a;

    else
         cout<<"It is a negative no."<<a;

    return 0 ;
}
